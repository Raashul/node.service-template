'use strict';

const route = require(`${__base}/app/routes/config/constants`);

module.exports = {
  role: 'ADMIN',
  description: "Administrator",
  access: {
    POST: [
    ],
    GET: [
    ],
    PUT: [
    ],
    DELETE: [
    ]
  }
};
